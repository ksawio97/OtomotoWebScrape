# Otomoto websraping
In this project I webscrape otomoto for car offers data

## **Installing necessary packages**
When navigated to project folder in cmd execute:

    pip install -r requirements.txt